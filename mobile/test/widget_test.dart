// Moongate widget tests.
// Run with: flutter test
//
// Full integration tests require a live Klipper + Moonraker instance.
// Unit/widget tests live here; add them as the codebase grows.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — real tests to be added.
  test('placeholder', () => expect(true, isTrue));
}
